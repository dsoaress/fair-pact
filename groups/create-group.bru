meta {
  name: create-group
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/groups
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "name": "Group test",
    "currency": "BRL"
  }
}
