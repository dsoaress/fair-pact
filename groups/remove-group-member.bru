meta {
  name: remove-group-member
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/groups/{{FIRST_GROUP_ID}}/remove-group-member
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "userId": "{{SECOND_USER_ID}}"
  }
}
