meta {
  name: update-group
  type: http
  seq: 7
}

patch {
  url: {{BASE_URL}}/groups/{{FIRST_GROUP_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "name": "Group test updated",
    "currency": "USD"
  }
}
